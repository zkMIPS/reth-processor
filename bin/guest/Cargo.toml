[workspace.package]
[package]
name = "reth"
description = ""
edition = "2021"

[profile.release]
opt-level = 3
codegen-units = 1
lto = "fat"
embed-bitcode = true
debug = true

[dependencies]
bincode = "1.3.3"

# workspace
guest-executor = { path = "../../crates/executor/guest" }

# Ziren
zkm-zkvm = { git = "https://github.com/ProjectZKM/Ziren" }
# zkm-zkvm = { path = "../../../Ziren/crates/zkvm/entrypoint" }

# Statically turns off logging
log = { version = "0.4", features = ["max_level_off", "release_max_level_off"] }
tracing = { version = "0.1", features = ["max_level_off", "release_max_level_off"] }

[patch.crates-io]
# Precompile patches
sha2 = { git = "https://github.com/ziren-patches/RustCrypto-hashes", branch = "patch-sha2-0.10.9", package = "sha2" }
bn = { git = "https://github.com/ziren-patches/bn", branch = "patch-0.6.0", package = "substrate-bn" }
k256 = { git = "https://github.com/ziren-patches/elliptic-curves", branch = "patch-k256-0.13.4" }
p256 = { git = "https://github.com/ziren-patches/elliptic-curves", branch = "patch-p256-0.13.2" }
alloy-primitives-v1-4-1 = { git = "https://github.com/ziren-patches/core.git", package = "alloy-primitives", branch = "patch-alloy-primitives-1.4.1" }

# The guest's zkvm crates come from the canonical Ziren checkout next to this
# repo (feat/upgrade-plonky3), the same way ziren-gpu consumes it: every
# zkm-zkvm / zkm-lib in the graph (the precompile patches declare their own,
# unbranched, which cargo cannot redirect to another branch of the same URL)
# resolves to this one source, so there is exactly one global allocator and
# the word-wise memcmp / zero-copy keccak reach every caller.
[patch."https://github.com/ProjectZKM/Ziren"]
zkm-zkvm = { path = "../../../Ziren/crates/zkvm/entrypoint" }
zkm-lib = { path = "../../../Ziren/crates/zkvm/lib" }
